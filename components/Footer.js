const Footer = () => {
  return (
    <footer style={{
      textAlign: 'center',
      maxWidth: '1080px',
      margin: 'auto',
    }}>
      <p>
        @NGELAG.SITE
      </p>
    </footer>
  );
};

export default Footer;
